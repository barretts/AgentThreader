{
  "name": "agent-threader",
  "version": "2.0.14",
  "description": "AgentThreader static release artifacts and installers",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "node install.js",
    "setup:all": "node install.js --all",
    "install-skills": "node install.js --skills-only",
    "uninstall-skills": "node install.js --uninstall"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
